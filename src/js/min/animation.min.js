const canvas=document.querySelector("canvas"),ctx=canvas.getContext("2d");canvas.width=window.innerWidth,canvas.height=window.innerHeight,ctx.fillStyle="black",ctx.fillRect(0,0,canvas.width,canvas.height),ctx.lineJoin="round",ctx.lineCap="round",ctx.lineWidth=200,ctx.globalCompositeOperation="destination-out";let isDrawing=!1,lastX=0,lastY=0;function draw(t){isDrawing&&(ctx.beginPath(),ctx.moveTo(lastX,lastY),ctx.lineTo(t.offsetX,t.offsetY),ctx.stroke(),[lastX,lastY]=[t.offsetX,t.offsetY])}canvas.addEventListener("mousedown",t=>{isDrawing=!0,[lastX,lastY]=[t.offsetX,t.offsetY]}),canvas.addEventListener("mousemove",draw),canvas.addEventListener("mouseup",()=>isDrawing=!1),canvas.addEventListener("mouseout",()=>isDrawing=!1);